# 待整合模块清单

> **用途**：登记「已经做成、但还没进主程序」的能力，避免遗漏或重复开发。
> **主程序** = `蚁国修改器.exe`（GUI 源码在 `mod/gui/`，打包脚本 `mod/scripts/build_exe.py`）。
> 每条写清：状态 / 成品在哪 / 怎么用 / **整合步骤** / **关键事实（免得重新踩坑）** / 验证证据。
> 有新进展就更新本文件并改日期，别另开新文件。

**状态图例**：✅ 已验证可直接整合 ｜ ⚠️ 有前置条件未满足 ｜ ❌ 结论已被推翻

---

## 🔒 禁止清理的核心文件保护清单（2026-09-16 23:35 用户要求）

> **任何"整理/清洁项目空间"的动作都不得删除或归档下列文件。**
> 它们是「已做成但还没进主程序」的能力本体 —— 丢了就要从头重做。
> 每次清理前先跑一遍上面的核对（应有 **22 项、0 缺失**）。

### A. 待整合模块 #1 关卡内食物
```
mod/食物自动补给.bat
mod/scripts/food_chamber.py      ← 主逻辑（定位/校验/fill/set/hold/lock）
mod/scripts/try_food.py
mod/scripts/ue_walk.py           ← UE4 对象遍历（两个模块共用）
mod/gui/core/memscan.py          ← 内存引擎（已共用，整合时不用搬）
```

### B. 待整合模块 #2 运行时蚁皇浆
```
mod/运行时补满蚁皇浆.bat
mod/补满蚁皇浆.bat               ← 存档层入口（需游戏关闭），与上面互为备份
mod/scripts/jelly_live.py        ← 主逻辑（锚点定位）
mod/gui/core/live.py
mod/gui/core/winproc.py          ← 铁律：外部命令唯一出口
```

### C. 表格生成链（双击 `更新表格.bat`）
```
mod/更新表格.bat
mod/scripts/update_all.py        ← 依次调用下面 4 个
mod/scripts/build_field_lib.py
mod/scripts/gen_jelly_table.py
mod/scripts/gen_ngp_report.py
mod/scripts/merge_html.py
mod/scripts/field_lib.py
mod/scripts/build_official_overrides.py
mod/蚁皇浆升级总表.html           ← 用户明确要求保留的产物
mod/升级项字段库.csv              ← 升级项的唯一权威源
```

### D. 主程序本体
```
mod/gui/**                       ← 全部源码（main.py / core/ / pages/ / theme/）
mod/dist/蚁国修改器.exe           ← 已交付的成品
mod/scripts/build_exe.py         ← 打包脚本
mod/蚁国修改器.bat                ← 开发态入口（改界面不用打包）
```

### 允许清理的（今天已归档的例子）
调试截图、`*.log`、`__pycache__`、`savetest/` 测试存档副本、临时探针脚本 `_probe_*.py`
→ 但一律**先移动到 `mod/backup/` 下的日期目录**，不要直接删。

---

## 1. ⚠️ 关卡内食物补满（**结论已被用户推翻，待重新验证**）

### ✅✅ 2026-09-17 00:40 最终解法 = 官方作弊开关（用户实测有效）
**与其跟经济系统搏斗，直接用开发者留在游戏里的调试开关**（SDK 官方字段名证实）：
```
UGTileGrid（= 我们一直叫"殖民地"的对象，vtable RVA 0x3CCDDB0，取容量最小者）：
  +0x63A InfinateResources  无限资源
  +0x63B FreeHatch          免费孵化
  +0x63C InstantBuild       秒建
  +0x63D InstantDig         秒挖
（4 个连续的 bool 字节，一次 int32 写 0x01010101 全部置 1）
```
- 工具：`scripts/cheat_flags.py status | on [秒] | off`；双击入口 `mod/关卡作弊开关.bat`
  （保持打开 + 换关自动重开 + 关卡结束返回码 10 自动收工）
- 用户实测：建造成功 ✓
- **为什么必须走这条路**（死路清单，别再试）：
  ① 界面顶栏数字 = `UGTileGrid.ResourcesAvailable(+0x4FC)`，**只是显示副本，建造判定不读它**
    （钉住 70 判定仍报"食物不足"，监视器证明游戏也不改回它）
  ② 假数据有副作用：仓库"满"会让工蚁停止采集（累计采集 5 分钟不动）
  ③ 内存布局**每关/每局都重排**（同一签名的对象集合都不同）→ 手工猜偏移不可维护
- SDK 生成：`mod/tools/dumper7/inject.py`（Dumper-7 v7.0.1 官方 DLL，注入 9408 成功，
  输出 `C:\Dumper-7\4.27.2-16902+++depot+EotU_PC-EotU\GObjects-Dump-WithProperties.txt`，
  含每个字段官方名+精确偏移 —— **这是以后一切定位的第一站，别再手工猜**）
- ✅ **已整合进 GUI（2026-09-17 01:50）**：`gui/core/cheats.py`（定位+读写，只依赖 memscan）
  + `gui/pages/cheats.py`（4 个开关，后台线程 1 秒回读同步）；
  主题已全局切黑色（`gui/theme.py`）；旧页面「蚁皇浆加点」改名「加点详情」
- 打包：`dist/蚁国修改器.exe`（17.7MB），打包态 --selftest 通过，实拍界面确认黑色主题生效
- ✅ **蚁皇浆运行时补满也已回归 GUI**（2026-09-17 02:00，用户要求）：在「关卡作弊」页下方，
  `gui/core/cheats.py::find_play_colony/refill_jelly`（jelly_live 同款打分锚点）。
  ⚠️ 注意区分两层：GUI「加点详情」页的补满 = **存档层**（需关游戏+重读档）；
  「关卡作弊」页的补满 = **运行时层**（游戏开着即时生效）——给用户讲解时别说混

### ⚠️⚠️ 2026-09-16 23:20 用户判定（历史记录）
- 用户原话：**「错大错特错」＋「连前三关都用不了」＋「没有效果」**
- 用户在 **1-1-NewHome** 实测：工具读到 `食物 70/70` **与界面顶栏完全一致**，
  但脚本 197 秒里**回写 0 次**（逻辑是"低于阈值才补"，而该关食物本就是满的）→ 表现为毫无变化
- ❌ **下面第 3 条"两本账必须同时给"的结论已作废** —— 用户在食物天然满值时**仍然建不了**
- ❌ 第 1 条里"格子池 `+0x290`/`+0x294` = 现存/上限"的标定**未证实**
- ✅ 仍然成立：殖民地 `+0x4FC` == 界面顶栏食物数字（多次 1:1 吻合）
- **重新开工前必须先问用户：什么样的表现才叫"有效果"**

### 原记录（保留备查，但状态条不再可信）

### 状态
- ⚠️ **"已验证成功"的说法已作废**（见上面 23:20 那条）。这里保留原实现，**代码不要删**，
  但**不要把它当可用功能对外交付**。
- 原入口：双击 `mod/食物自动补给.bat`（现已指回 `food_chamber.py hold`）
- 原实现「无任何硬编码地址，冷启动 0.3 秒重新定位」这一点本身是成立的
- 📦 **2026-09-16 晚间的新实现已回退**，代码完整保存在
  `mod/backup/2026-09-16_晚间食物线_回退/scripts/`：
  `food_supply.py`（report / hold 持续补给 + 身份核对 + 关卡跟随）、
  `watch_food_readonly.py`（只读监视）、`shot_burst.py`（连拍取证）
  → **重新开工时先从这里取回，别从头写**

### 成品与依赖
| 文件 | 作用 |
|---|---|
| `mod/食物自动补给.bat` | 双击入口（= `hold 86400 99`，关窗口即停） |
| `mod/scripts/food_chamber.py` | 主逻辑：定位 / 校验 / fill / set / hold / lock / 关卡结束自检 |
| `mod/scripts/ue_walk.py` | UE4 对象遍历（GWorld → ULevel → Actor 表） |
| `mod/scripts/try_food.py` | 连接 / 日志 / 扫描小工具（**已经在用 `gui/core/memscan.py`**） |
| `gui/core/memscan.py` | 内存引擎 —— **已共用，整合时不用搬** |

### 现有能力（命令行）
`report` 只读核对 ｜ `fill` 补满 ｜ `set <值>` 指定 ｜ `hold [秒] [百分比]` 智能补给 ｜ `lock [秒]` 无条件锁住

### ★ 关键事实（整合时逐条对照，少一条就出 bug）
1. **食物是两本账，必须同时给**：粮仓格子 `+0x290`（现存）/ `+0x294`（每格上限），
   殖民地账本 `+0x4FC`（总量）/ `+0x500`（总容量）。
   只给一边 → **界面数字会变，但建不了东西**（这就是当初"中看不中用"的根因）。
2. **游戏会周期性重算覆盖**：它持有自己的一份权威计数并往下推 →
   **单次 fill 会在几秒内被改回**；只有 `hold` / `lock` 的持续回写能压住。
3. **粮仓横跨多个类**（本局 7 格 + 9 格 = 容量 160）→ 必须**组合匹配**：
   先用账本拿总容量，再反查"哪几类的上限和正好等于它"，并优先选"现存之和也对得上"的那组。
4. **写入前必须核对对象身份**：定位时记 `[vtable@+0x00, ClassPrivate@+0x10]` 指纹，
   每次写入前比对；对不上（对象已释放、内存被复用）→ **立刻停写并重新定位**。
   见 `food_chamber.py::verify()`；**老缓存没有 idents 字段必须判失效**。
5. **定位走 Actor 表**（0.01 秒），**禁止**用全内存字节扫描（曾退化到 175 秒）。
6. **关卡结束自动收工**：`level_fingerprint() = (UWorld, ULevel, Actor 数)`；
   世界/关卡换人、读不到且进程已退出、连续 6 次定位失败 → 返回码 **10**（bat 收到自动关窗）。
7. 每格上限 10（拆一格容量 −10）；**别把别的资源类当粮仓**（本局另有 23 格 × 上限 4 = 92 的另一类资源，不要动）。

### 整合到 GUI 的步骤
1. `scripts/ue_walk.py` → `gui/core/uewalk.py`（原样搬，改 import）
2. 新建 `gui/core/food.py`：从 `food_chamber.py` 抽出
   `locate / verify / fill / set_total / relocate / level_fingerprint`，
   去掉 CLI 与 print，改成「返回结构化结果 + 可选日志回调」；
   **只依赖 `core/memscan.py` + `core/uewalk.py`，不要依赖 `try_food.py`**
3. 新建 `gui/pages/food.py`：
   - **「补满一次」**按钮 → `food.fill()`
   - **「自动补给」**开关（后台线程跑 hold 循环；阈值可调；显示已补次数）
   - 实时状态：粮仓格数 / 现存合计 / 账面（走 report 只读路径）
   - 关卡结束自动停：轮询 `level_fingerprint()`，换人即停并提示
4. `gui/main.py` 的 `PAGES` 注册表加「食物」页
5. **铁律**：外部命令只能从 `core/winproc.py` 出（`CREATE_NO_WINDOW`）；
   GUI 内禁止 `import subprocess`（`--selftest` 用 ast 扫源码检查）
6. 打包：`scripts/build_exe.py` 的内嵌清单**不用改**（新模块是 .py，被 import 就会进 exe）
7. 收尾：`--selftest` 加一项「食物模块可导入 + 能只读定位」；开发态 + 打包态各跑一遍

### 验证证据（2026-09-16）
- 用户实测：账面 0/70 + 仓库空 → 建不了；补满后**能建造**（终审通过）
- 自动定位 **0.316 秒**；`fill` 走缓存 0.3 秒；`hold 20 80` 20 秒补 11 次，末尾 160/160
- 身份闸门离线单测 3 例全过：正常写入 / 对象被换人→拒写 / 老缓存判失效
- 关卡结束自动退出 4 条路径全过：换人 0.0s、进程没了 2.0s、连续找不到 2.5s、正常不误退

---

## 2. ✅ 运行时蚁皇浆补满（2026-09-16 21:30 完成 —— **新档也实测通过**）

### 状态
- **✅ 已做成 bat 并实测通过**：双击 `mod/运行时补满蚁皇浆.bat`（游戏开着就能用，不碰存档文件）
- **不依赖浆的值** → **新建档（浆=0 + 零加点）同样可用**，这是它相比旧方式的关键升级
- 定位耗时 **约 1 秒**；写入后内存回读 + 游戏存盘自动落档

### 成品与依赖
| 文件 | 作用 |
|---|---|
| `mod/运行时补满蚁皇浆.bat` | 双击入口（默认走锚点定位；失败才转"手输面板数字"） |
| `mod/scripts/jelly_live.py` | 主逻辑：锚点定位 / `--auto` 旧法 / 手动 / `--check` / 写入前复核 |
| `mod/scripts/ue_walk.py` | UE4 对象遍历（UWorld → ULevel → Actor 表） |
| `gui/core/live.py` | 读写引擎（`game_proc / known_addon_values / write_jelly / JELLY_CAP`） |
| `mod/补满蚁皇浆.bat` | **存档层**入口（需游戏完全关闭）—— 两条路互为备份 |

### ★★★ 锚点定位（最终方案）
**蚁皇浆 = 殖民地对象 + `0x89C`**（常量 `scripts/jelly_live.py::JELLY_OFF`）

- **怎么认殖民地对象**（与浆的值无关 → 新档也能用）：
  遍历 `ULevel+0x98` 的 Actor 表 → **vtable 的 RVA == 0x3CCDDB0**（跨关卡实测一致：
  `LabFormicarium` 与 `LabFormicariumStage2` 都是）；兜底判据 = 食物记录签名
  `[食物, 容量, 0×16, 1.0f]`（`+0x4FC/+0x500`）
- **一关里有 2 个殖民地对象** → 用 `_score()` 打分挑"玩家在玩的那个"：
  累计采集(+0x86C) > 0 → +3 ｜ 已挖掘(+0x8B4) > 0 → +2 ｜ 加点等级 ≥3 种 → +2 ｜ 蚁皇浆 > 0 → +1。
  实测真身 **8 分** / 另一个 **1 分**（采集 0、挖掘 0）→ 稳定选中
- **写入前复核对象身份**：读 `addr − 0x89C` 的 vtable，RVA 不是 0x3CCDDB0 就**拒绝写入**
  （实测堆地址会随游戏内部重建而变 —— 两次连续定位的地址就不同）
- **标定依据（三重验证）**：① 与 aa 档的殖民地对象 **class/vtable 完全相同**
  ② 食物记录签名命中 ③ **蚁皇浆 − 340 = 85** —— 正是 20:35 记录过的加点等级值，**分毫不差**

### 其他字段（同一次标定，备查）
```
+0x4FC 食物   +0x500 容量   +0x86C 累计采集资源   +0x8B4 已挖掘格数
+0x89C 蚁皇浆 +0x8B8 人口上限(疑似)   +0x19A0 游戏时长(float)
```

### 返回码约定
`0` 成功 ｜ `1` 用法/游戏没开 ｜ `4` 定位失败（bat 转手动输入）｜ `5` 回读不符。
（旧版的 `2 浆为0` / `3 无加点点数` 已不需要 —— 锚点模式不依赖这两个）

### 事故留痕（必读）
用**猜的 137** 试跑时，旧的扫值引擎仍"定位成功"，把 20 亿写到一张编号表上（已还原原值，未触碰存档）。
**铁律：绝不用"猜的值"当输入。** 锚点模式从原理上避免了这类误判。

### ★ 落档验证（2026-09-16 20:33）
运行时写入的 **2,000,000,000 已被游戏存进 `Colony1LevelData.sav`**（两处 RoyalJelly 都是 20 亿）
→ "运行时改 → 游戏自己落档"链路闭合，有文件为证。

### 验证证据
| 场景 | 结果 |
|---|---|
| pp / Colony1（浆 1,999,999,666） | ✅ 先定到地址，写入 20 亿，面板实测显示 |
| pp / Colony1（浆 20 亿，第 2 次） | ✅ 候选 2 个 → 打分 8 : 1 → 选中真身 |
| **Colony3（今天新建，浆 0 + 零加点）** | ✅ **定位成功（读到 0）→ 写入 20 亿 → 回读 OK** ← 新档可用 |
| 非关卡界面 / 加载中 | ✅ 明确报"没找到、未写入"，不乱写 |

### 整合到 GUI 的步骤
1. `scripts/jelly_live.py` 的锚点部分 → 抽成 `gui/core/jelly_live.py`
   （`JELLY_OFF` / `COLONY_VT_RVA` / `find_colony` / `_score`；只依赖 `core/memscan.py` + `core/uewalk.py`）
2. 新建 `gui/pages/jelly.py`：「补满蚁皇浆」按钮 + 当前值显示 + 定位失败提示
3. `gui/main.py` 的 `PAGES` 注册表加「蚁皇浆（运行时）」页
4. 铁律同第 1 条（外部命令只走 `core/winproc.py`；`--selftest` 加导入检查）
5. 打包清单不用改

> ⚠️ **注意 GUI 里的双入口**：蚁皇浆有两套（**存档层** = 需退游戏；**运行时** = 需游戏开着）。
> 界面上要写清各自前提，否则用户会混淆（今天已经踩过一次："工具失效"其实是游戏没关）。
