# 地下蚁国 Mod —— 首次打通记录

日期：2026-09-14

---

## 一、结论：卡点解除了，全链路已跑通

之前卡在「游戏官方 pak 的索引被 AES 加密，拿不到文件清单」。
**这次的思路绕开了它：直接拿别人做好的 mod pak 当参照物。**

原因很简单 —— **mod pak 本身不加密**。作者用 UnrealPak 打包时没开加密，
所以里面的**完整资产路径**和**完整资产数据**，我们都能直接读。

**已实测走通的全流程：**

```
解包 mod pak → 拿到 70 个文件的真实路径
   → 解析物种数值表（281 行 × 34 列，全部读出）
   → 就地改数值
   → repak 重新打包（70 文件，尺寸与原包一致）
   → 再解包读回，改动确认生效
```

**结论：替换型 mod 已经具备完整实现能力，不再有任何未知卡点。**

---

## 二、这个 mod（BushCricketColony）是什么

**玩法**：把游戏里的「小黑蚁」蚁群，整体替换成**刺魔螽斯（Spiny Devil / Bush Cricket）**。

这正好对应你在 B 站看到的「把螽斯做成玩家操控的生物」。
对照名称表可见：

| 游戏内部 ID | 原始显示名 | 该 mod 改成的显示名 |
|---|---|---|
| `LittleBlackAnt` | 小黑蚁（兵蚁） | **Large spiny devil**（大刺魔） |
| `LittleBlackAntWorker` | 小黑蚁工蚁 | Baby spiny devil（小刺魔） |
| `TrapjawAnt` | 陷阱颚蚁 | Medium spiny devil（中刺魔） |
| `SlaveMakerAnt` | 蓄奴蚁 | Small spiny devil（小刺魔） |

**它改了哪些东西**（70 个文件分四类）：

| 类别 | 路径 | 作用 |
|---|---|---|
| 数值表 | `EotU/Content/Assets/Data/*.uasset/.uexp` | 8 张表：物种数值、技能、效果、花费、关卡等级 等 |
| 蓝图 | `EotU/Content/Blueprints/Creatures/**` | 生物本体（模型/行为）：小黑蚁、陷阱颚蚁、蓄奴蚁 |
| 图标 | `EotU/Content/UI/InGame/.../CharacterIcons/**` | 单位头像（信息面板 + 方形图标） |

**关键发现**：它**整表替换**了 `CreatureStats`（281 行全都打包进去了），
而不是只塞改动的那几行。所以这个 mod pak 里，**躺着游戏全物种的完整数值表**。

---

## 三、物种是怎么定义的（终于有确定答案）

### 3.1 数值 = 一张数据表

`EotU/Content/Assets/Data/CreatureStats` 是一张 UE4 DataTable，
**281 行 × 34 列**，每行是一种生物的一个等级版本，例如 `BlackAnt1/2/3`（黑蚁 1/2/3 级）、
`LittleBlackAnt1/2/3`、`WorkerAnt1/2/3`、`Queen1` 等，共 **170 种**不同生物条目。

**34 个字段（全部列出）：**

- 生命：`Health`（血量）、`HealthPerSecond`（回血速度）
- 攻击：`AttackDamage`、`AttackSpeed`、`BasicAttackThreatGeneration`（仇恨值）
- 防御：`Armour`、`PhysicalResist`、`VenomResist`、`SlowResist`、`AOEResist`、`Evasion`、`MaxSingleDamage`
- 移动：`Speed`、`WanderDistance`、`ArrivalDistance`、`ReachableDistance`、`AttackTolDistance`、`TargetRadius`
- 采集：`DigAmmount`、`DigSpeed`（挖掘）、`FoodCarryMax`、`FoodHarvestTime`、`FoodValue`（食物）、`PickupTime`
- 其他：`PointValue`（分数）、`Piercing`、`Temperament`（性格，见下）
- 免疫开关（7 个）：`CannotBeConfused`、`CannotBeStunned`、`CannotBeKnockedBack`、
  `CannotBePickedUp`、`CannotBeStrongPickedUp`、`CannotBeExercuted`（不可处决）、`CannotFlee`

`Temperament`（性格）取值：`AttackOnSight`（见敌就打）、`AttackWhenAttacked`（被打才还手）、
`AttackWhenFavorable`（有利才打）、`DoNotAttack`（不主动攻击）、`RetreatWhenAttacked`（被打就跑）。

### 3.2 名字和描述 = 另一张对照表

`EotU/Content/Assets/Data/CreatureInformation`，614 条键值对。
键的格式是 `CreatureInfo_<内部ID>_DisplayName` / `_ScientificName` / `_ShortDescription` / `_Level3Description`。
**改显示名 ⚠️ 比改数值难**（字符串长度会变，需要重排整张表），数值则是定长 4 字节、直接原地改。

### 3.3 特殊能力不在数值表里

数值表只管「基础属性」。像木蚁的远程射击这类**特殊技能**，存在
`Abilities` / `Creatures_Abilities` / `Abilities_Effects` / `Effects` 这几张独立表里 ——
这跟贴吧那位作者的经验一致（"改本体无效，要去改独立的射击技能资产"）。

---

## 四、产物清单

| 文件 | 说明 |
|---|---|
| `物种数值表_CreatureStats.csv` | **281 行 × 34 列全物种数值**（Excel 可直接打开） |
| `物种名称表_CreatureInformation.csv` | 614 条名称/学名/描述对照 |
| `BushCricketColony_P.pak` | 重新打包的 mod（已做修改：刺魔血量/攻击大幅提升） |
| `origin/BushCricketColony_P.pak` | 你下载的原始 mod 包（**未被改动，留着做对照**） |
| `extracted/` | 原 mod 解包后的 70 个资产文件（干净原件） |
| `build/` | 已修改的资产副本（打包源） |
| `scripts/` | 本次写的解析/导出/打包脚本（可复用） |

### 本次演示改了什么

| 生物 | 字段 | 原值 | 改后 |
|---|---|---|---|
| `LittleBlackAnt1`（大刺魔） | Health | 400 | **1200** |
| `LittleBlackAnt1` | AttackDamage | 20 | **50** |
| `LittleBlackAnt2` | Health | 440 | **1400** |
| `LittleBlackAnt2` | AttackDamage | 22 | **55** |
| `LittleBlackAnt3` | Health | 500 | **1800** |
| `LittleBlackAnt3` | AttackDamage | 25 | **70** |

> 这只是为了验证链路而随手做的一个改动，**不是最终需求**。要改什么由你定。

---

## 五、安装 / 卸载（⚠️ 需你同意后再动）

**安装位置**：把 `.pak` 文件放进
`D:\app\steam\steamapps\common\Empires of the Undergrowth\EotU\Content\Paks\`
（该目录目前只有官方的 `pakchunk0-WindowsNoEditor.pak`，往这里**新增**文件即可）

**卸载**：把放进去的那个 `.pak` 删掉，游戏立刻恢复原样。**不改动任何官方文件。**

⚠️ 注意：如果同时装了这个 mod 和 `BushCricketColony_P.pak`，两者文件名相同会冲突 ——
只能留一个。所以请**用我们的包替换原 mod 包**，不要两个并存。

---

## 六、下一步（等你定）

1. **改什么** —— 数值？显示名？还是把某个生物换成另一种？
2. **要不要真的装进游戏测试** —— 需要你同意往游戏目录放文件
3. 可选进阶：
   - 改显示名（需要处理字符串长度变化）
   - 改技能/效果表（`Abilities` 等 4 张表）
   - 从零做**自己的**独立 mod pak（只装改动的那几个文件，不整表替换）

---

## 七、技术备注（供后续复用）

### pak 结构（本游戏）
- 版本 `V8B`，footer **204 字节**（`magic + version + offset + size + hash(20) + 压缩方式表(5×32)`）
- **官方 pak**：索引 AES 加密（需密钥，未获得）
- **mod pak**：索引未加密（`encrypted index: false`），可直接读写
- 挂载点 `../../../`（对应 `EotU/`）

### uasset / uexp 结构（UE 4.26/4.27）
- 名字表：`FString + 8 字节哈希` 连续排列
- DataTable 的 uexp 布局：
  ```
  [UObject 的 tagged properties]   ← RowStruct 等，以 FName "None" 结束
  int32 (恒为 0)
  int32 NumRows
  NumRows × { FName 行名, tagged properties…, FName "None" }
  ```
- 单个属性的 tag 布局（**关键，顺序容易搞错**）：
  ```
  FName  属性名
  FName  类型名
  [StructProperty: FName 结构名]  /  [BoolProperty: 1 字节值]
  int32  Size
  int32  ArrayIndex
  [EnumProperty / ByteProperty: FName 枚举名]   ← 在 ArrayIndex 之后！
  [ArrayProperty: FName 元素类型]
  1 字节 是否有属性 GUID
  Size 字节 数值本体
  ```
- 浮点数 = 定长 4 字节 → **可以直接原地改写，不影响文件长度**（本次就是这么做的）

### 环境坑（重复遇到，记下）
- Bash 里 PATH 默认为空，每条命令前需 `export PATH="/c/Windows/System32:/c/Windows:/usr/bin:/bin"`
- Windows 自带 `tar.exe`（bsdtar 3.8.8）**能解 RAR5**，但只认 `C:/` 风格路径，不认 `/c/...`
- repo 里 `find` 会被 Windows 的 `find.exe` 抢走 → 用 `/usr/bin/find`
- 同目录下 `curl` 不认 `/d/...` 路径 → 先 `cd` 再用相对路径
- `UAssetGUI` 需要 .NET 8 桌面运行时（本机只有 5.0 / 9.0）→ 未用上，改用了自写解析器
